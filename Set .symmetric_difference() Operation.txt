no_of_english=int(input())
english=set((input()).split())
no_of_french=int(input())
french =set((input()).split())

sum=0
for i in english:

    for j in french:

        if i==j:
            sum+=1
            
print((len(english)+len(french))-(sum*2))
            
